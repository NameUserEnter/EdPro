@model EdPro.Models.EpSubjectLoutcome

@{
    ViewData["Title"] = "Edit";
}

<h1>Редагування</h1>

<h4>Навчальний результат предмету</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" value="@ViewBag.SubjectId" name="SubjectId" />
@*            <div class="form-group">
                <label asp-for="SubjectId" class="control-label"></label>
                <select asp-for="SubjectId" class="form-control" asp-items="ViewBag.SubjectId"></select>
                <span asp-validation-for="SubjectId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="LearningOutcomeId" class="control-label"></label>
                <select asp-for="LearningOutcomeId" class="form-control" asp-items="ViewBag.LearningOutcomeId"></select>
                <span asp-validation-for="LearningOutcomeId" class="text-danger"></span>
            </div>
            <p></p>
            <div class="form-group">
                <input type="submit" value="Зберегти" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <p class="text-danger">@ViewData["ErrorMessage"]</p>
</div>

@Html.ActionLink("Повернутись","Index", new {id = ViewBag.SubjectId, name = ViewBag.SubjectName, eprogramId = ViewBag.SubjectEprogramId, credit = ViewBag.SubjectCredit, controlId = ViewBag.SubjectControlId}, new { @class = "btn btn-secondary" })

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
