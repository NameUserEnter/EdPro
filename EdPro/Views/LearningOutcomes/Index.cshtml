@model IEnumerable<EdPro.Models.LearningOutcome>

@{
    ViewData["Title"] = "Index";
}

<h1>Навчальні результати</h1>

<p>
    <a asp-action="Create">Додати</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LearningOutcome1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Loname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Speciality)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LearningOutcome1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Loname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Speciality.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Редагувати</a> 
                <p> </p>
                <a asp-action="Details" asp-route-id="@item.Id">Небхідні предмети</a>
                <p> </p>
                <a asp-action="Delete" asp-route-id="@item.Id">Видалити</a>
            </td>
        </tr>
}
    </tbody>
</table>
