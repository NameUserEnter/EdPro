@model EdPro.Models.CompetencesType

@{
    ViewData["Title"] = "Create";
}

<h1>Додавання типу компетентності</h1>

<h4>Тип компетентності</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CompType" class="control-label"></label>
                <input asp-for="CompType" class="form-control" />
                <span asp-validation-for="CompType" class="text-danger"></span>
            </div>
            <p></p>
            <div class="form-group">
                <input type="submit" value="Додати" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <p class="text-danger">@ViewData["ErrorMessage"]</p>
</div>

<div>
    <a asp-action="Index"><button type="button" class="btn btn-secondary">Повернутися</button></a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
